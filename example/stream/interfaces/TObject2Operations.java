package example.stream.interfaces;

import example.objects.DAddr;

import java.util.List;

public interface TObject2Operations {

    /* [List] collect(groupingby (mapping)) */
    void tListGroupingCountry(List<DAddr> Objs);
}
